import MessageLink from '../components/messagelink'

const ResetSuccessPage = (): JSX.Element => <MessageLink buttonLabel="Sign In Now" href="/signin" message="Password reset success!" />

export default ResetSuccessPage
