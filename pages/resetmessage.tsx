import MessageLink from '../components/messagelink'

const ResetMessagePage = (): JSX.Element => <MessageLink buttonLabel="Home" href="/" message="Follow the link in your inbox to reset your password!" />

export default ResetMessagePage
