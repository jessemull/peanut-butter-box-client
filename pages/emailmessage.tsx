import MessageLink from '../components/messagelink'

const EmailMessagePage = (): JSX.Element => <MessageLink buttonLabel="Home" href="/" message="Follow the link in your inbox to activate your account!" />

export default EmailMessagePage
